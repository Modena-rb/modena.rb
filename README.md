# Modena.rb

Sito del meetup Ruby di Modena. Deployato su [modenarb.dev](https://modenarb.dev).

Ogni post in `_posts/` corrisponde a una serata.

## Sviluppo

```bash
docker compose up
```

Apri `http://localhost:4000`.
